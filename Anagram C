#include<stdio.h>
int main()
{
//Try out your code here
  char str1[10];
  char str2[10];
  char testchar;
  gets(str1);
  gets(str2);
  int len=strlen(str1);
  int i,j,flag=0;
  for(i=0;i<len;i++)
  {
  	testchar=str2[i];
    printf("%c\n",testchar);
    for(j=0;j<len;j++)
    {
      printf("%c\n",str1[j]);
    	if(testchar==str1[j])
        {
          	flag=1;
          	break;
        }
        else
        {
        	flag=0;
        }
    }
  if(flag==0)
    break;
  }
  if(flag==1)
    printf("Anagram");
  else
    printf("Not an Anagram");
return 0;
}
